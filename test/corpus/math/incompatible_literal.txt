; ================================================================================
; NOT IMPLEMENTED Math Incompatible Literal ID=7QDA3VN
; ================================================================================
; 
; /// Expect:
; /// - error: "Binary arithmetic operation between incompatible types (‘u8’ and ‘u16’)\n"
; 
; fn main() {
;     let x: u8 = 12;
;     let y: u16 = 34;
;     println("{}", x + y);
;     println("{}", x - y);
;     println("{}", x * y);
;     println("{}", x / y);
; }
; 
; --------------------------------------------------------------------------------
; 
; (source_file)
